import { BytesLike } from 'ethers';
import { RemoteSigner } from './remote-signer';
import { ChainIdLike } from '@0xsequence/network';
export declare class GuardRemoteSigner extends RemoteSigner {
    isSequence: boolean;
    defaultChainId: number;
    private readonly _guardd;
    private readonly _address;
    constructor(address: string, hostname: string, isSequence?: boolean, defaultChainId?: number);
    signMessageWithData(message: BytesLike, auxData?: BytesLike, chainId?: ChainIdLike): Promise<string>;
    getAddress(): Promise<string>;
}
